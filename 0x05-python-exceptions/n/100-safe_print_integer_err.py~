#!/usr/bin/python3

def safe_print_integer_err(value):
    try:
        if not isinstance(value, int):
            raise Exception as e
        return False
        else:
            print("{}".format(value))
            return True
    except:
    
